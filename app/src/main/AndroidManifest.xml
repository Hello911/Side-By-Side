<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.sidebyside">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Setting" android:label="Setting"></activity>
        <activity android:name=".Photo1" android:label="Pick 1st photo"></activity>
        <activity android:name=".Photo2" android:label="Photo"></activity>
        <activity android:name=".Compare" android:label="Compare" android:screenOrientation="portrait"></activity>
        <activity android:name=".Share" android:label="Compare"></activity>

        <receiver android:name=".NotificationPublisher"/>
    </application>

</manifest>